<p style="color: green"><%= notice %></p>

<table class="table table-striped table-bordered table-hover">
    <thead class="thead-dark">
      <tr>    
        <th><%= render @entry %></th>
        <th><%= link_to "Edit", edit_entry_path(@entry), class: "btn btn-secondary" %></th>
        <th><%= link_to "Back", entries_path, class: "btn btn-secondary" %></th>
        <th><%= button_to "Delete", @entry, method: :delete, class: "btn btn-danger" %></th>
      </tr>
  </thead>

